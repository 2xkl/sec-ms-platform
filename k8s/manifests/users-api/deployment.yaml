apiVersion: apps/v1
kind: Deployment
metadata:
  name: users
  labels:
    app: users
spec:
  replicas: 2
  selector:
    matchLabels:
      app: users
  template:
    metadata:
      labels:
        app: users
    spec:
      containers:
      - name: users-api
        image: acrwedosmpdev.azurecr.io/users-api:14
        ports:
        - containerPort: 8080
      imagePullSecrets:
      - name: acr-secret
